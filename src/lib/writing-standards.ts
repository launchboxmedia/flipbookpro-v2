export const WRITING_STANDARDS = `
WRITING STANDARDS — apply to every word you write:

Never use these words or phrases: pivotal, crucial, seamlessly, leverage, unleash, game-changer, transformative, impactful, actionable, elevate, empower, navigate, cutting-edge, robust, holistic, resonate, vibrant, tapestry, delve, showcase (as verb), highlight (as verb).

Never invent statistics, percentages, or named research studies.
Never use bold text inside prose.
Never recap what was just said.
Never start with "In this chapter we will explore".
Write in second person, direct to the reader.
Short sentences for impact. Long sentences for context.
`.trim()

export const HUMANIZATION_PROMPT = `
Write like a knowledgeable person talking directly to the reader — not like a content generator. Vary sentence length. Use concrete specifics over abstract claims. Start sentences with "You", "When", "If", "Consider", not with "In", "The", or "This chapter".
`.trim()
